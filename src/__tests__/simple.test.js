// Basic tests without JSX
test('Jest is working', () => {
  expect(true).toBe(true);
});

test('Math works', () => {
  expect(2 + 2).toBe(4);
});

test('String operations', () => {
  expect('hello'.toUpperCase()).toBe('HELLO');
});
